# Dummy data mode — set to true to use local mock data instead of real API calls.
# Flip to false (or remove) once the backend is ready.
VITE_USE_DUMMY_DATA=true

VITE_API_URL=http://localhost:3000/api/v1
VITE_WS_URL=ws://localhost:3000
